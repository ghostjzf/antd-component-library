import { default as dateFilter } from './dateFilter';
import { default as orNotFilter } from './orNotFilter';
import { default as copyMe } from './copyMe';
import { default as downloadFile } from './downloadFile';
import { default as previewImg } from './previewImg';

const utils = {
    dateFilter,
    orNotFilter,
    copyMe,
    downloadFile,
    previewImg
};

export default utils;
